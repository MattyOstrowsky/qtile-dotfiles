(defpoll bluetooth :initial "" :interval "1m" "bash ~/.config/eww/scripts/bluetooth icon") 

(
  defwidget bluetooth [] 
  (
    button  :class "blue-btn"
            :tooltip "Bluetooth"
            :onclick "~/.config/eww/scripts/bluetooth run" 
    bluetooth
  )
)